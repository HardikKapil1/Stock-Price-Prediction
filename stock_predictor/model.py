"""Model building and inference helpers."""
from typing import Any


def build_model(input_shape: tuple) -> Any:
    """Return an uncompiled placeholder model object."""
    return None


def load_model(path: str) -> Any:
    """Load and return a trained model from `path`."""
    return None
